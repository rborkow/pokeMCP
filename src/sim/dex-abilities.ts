/**
 * Type-only stub standing in for pokemon-showdown's sim/dex-abilities (no runtime code).
 * See src/sim/dex-species.ts for why these stubs exist and stay loose.
 */
export type AbilityData = { [k: string]: any };
export type AbilityDataTable = { [id: string]: AbilityData };
