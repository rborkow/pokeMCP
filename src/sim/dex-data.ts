/**
 * Type-only stub standing in for pokemon-showdown's sim/dex-data (no runtime code).
 * See src/sim/dex-species.ts for why these stubs exist and stay loose.
 */
export type TypeData = { [k: string]: any };
export type TypeDataTable = { [id: string]: TypeData };
