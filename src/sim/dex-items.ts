/**
 * Type-only stub standing in for pokemon-showdown's sim/dex-items (no runtime code).
 * See src/sim/dex-species.ts for why these stubs exist and stay loose.
 */
export type ItemData = { [k: string]: any };
export type ItemDataTable = { [id: string]: ItemData };
