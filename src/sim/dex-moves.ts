/**
 * Type-only stub standing in for pokemon-showdown's sim/dex-moves (no runtime code).
 * See src/sim/dex-species.ts for why these stubs exist and stay loose.
 */
export type MoveData = { [k: string]: any };
export type MoveDataTable = { [id: string]: MoveData };
